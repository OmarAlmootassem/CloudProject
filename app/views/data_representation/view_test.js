'use strict';

describe('CloudApp.data_representation module', function() {

  beforeEach(module('CloudApp.data_representation'));

  describe('view1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ChartsCtrl = $controller('ChartsCtrl');
      expect(ChartsCtrl).toBeDefined();
    }));

  });
});